---
interface Props {
    name: string;
    description: string;
}

const { name, description } = Astro.props;
---

<div class="font-mono font-bold text-sm text-white text-center mb-1">{name}</div>
<div class="text-[11px] text-purple-200 text-center leading-tight">{description}</div>
